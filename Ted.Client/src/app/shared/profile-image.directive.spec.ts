import { ProfileImageDirective } from './profile-image.directive';

describe('ProfileImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileImageDirective();
    expect(directive).toBeTruthy();
  });
});
